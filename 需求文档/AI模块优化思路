AI Module Issues Analysis
一、工具调用失败的根本原因
核心问题出在 AlibabaOpenAiChatModel 的自定义实现与 Spring AI 标准工具调用管线的兼容性上。

问题 1：ToolCallingManager 是空的（最关键的 Bug）

在 AIConfig.java 中：


return new AlibabaOpenAiChatModel(openAiApi, options,
        ToolCallingManager.builder().build(),   // ← 空的 ToolCallingManager
        RetryUtils.DEFAULT_RETRY_TEMPLATE,
        ObservationRegistry.NOOP);
ToolCallingManager.builder().build() 创建了一个完全没有注册任何工具回调的实例。当 API 返回工具调用请求时，internalCall() 方法中的 this.toolCallingManager.executeToolCalls(prompt, response) 找不到对应的 ToolCallback 实现，导致执行失败。

问题 2：ChatClient 层的工具与 ChatModel 层的工具脱节

在 AIConfig.java 中，userChatClient 和 adminChatClient 通过 defaultTools(userTools) 配置了工具，但这一配置只影响 ChatClient 层的 Prompt 构建。关键在于：

defaultTools() 将 UserAITools/AdminAITools 中的 @Tool 方法解析为 ToolCallback 对象，存放在 ChatClient 的内部
当 ChatClient 调用 chatModel.call(prompt) 时，这些工具信息应该被编码到 Prompt 的 options 中
但在 AlibabaOpenAiChatModel.addToolDefinitions() 中，工具解析逻辑使用了 this.toolCallingManager.resolveToolDefinitions(requestOptions)，而这个 ToolCallingManager 是空的
问题 3：buildRequestPrompt 与 addToolDefinitions 之间的信息丢失

buildRequestPrompt() 将 runtime options 与 default options 合并后创建一个新的 Prompt 实例。但在合并过程中，从 ChatClient 传递下来的工具定义（ToolDefinition）可能因为 ModelOptionsUtils.merge() 的类型转换而丢失。

结论：工具定义从未真正被发送到 DashScope API，所以 AI 模型根本不知道有工具可用，自然不会发出工具调用请求。即使发出了调用请求，ToolCallingManager 也无法执行，因为没有任何回调注册。

二、其他功能性问题
问题 4：SeatReservationServiceImpl 中的类型不匹配（编译错误）

RedisSeatService 返回 Integer：


// RedisSeatService.java
public Integer reserveSeat(...) 
public Integer cancelReservation(...)
但 SeatReservationServiceImpl 却用 Long 接收：


// SeatReservationServiceImpl.java（lint 修改后）
Long result = redisSeatService.reserveSeat(...);    // 编译错误
Long result = redisSeatService.cancelReservation(...); // 编译错误
Java 不会自动拆箱 Integer 为 Long，这里必报编译错误。

问题 5：SeatMapper.getAvailableSeatList() 缺少 MyBatis 映射（运行时错误）

SeatMapper.java 声明了：


List<SeatListVO> getAvailableSeatList(@Param("status") String status);
但 SeatMapper.xml 中没有对应的 <select> 定义，也没有 @Select 注解。这会在运行时抛出 org.apache.ibatis.binding.BindingException。

问题 6：AdminAITools.getClassScores 语义错误


List<ScoreEntryListVO> scores = scoreEntryMapper.getScoreList(null, null, semesterId);
classId 参数传入但从未使用，实际查询的是整个学期的成绩而非特定班级。方法名与行为不一致。

问题 7：UserAITools.searchBooks 搜索范围不完整


List<BookListVO> books = bookMapper.getBookList(null, keyword, null, categoryId, null);
//                                         title↑          author↑
keyword 只匹配了 title 字段，用户按作者搜索时无法生效。

问题 8：BookRecommendService 和 CourseRecommendService 的 aiService 未使用

经过 lint/modification 后，这两个服务改用本地随机数据生成推荐，aiService 字段注入但从未使用，初始化逻辑冗余。

三、优化方案
方案 1：修复工具调用（最高优先级）
需要重写 AlibabaOpenAiChatModel 的工具解析逻辑，或者直接使用 Spring AI 官方 OpenAiChatModel。

推荐方案：删除自定义 AlibabaOpenAiChatModel，改用 Spring AI 标准 OpenAiChatModel，利用其内置的 DashScope 兼容层。修改 AIConfig.java：


@Configuration
public class AIConfig {

    @Bean
    @Primary
    public ChatModel chatModel(OpenAiApi openAiApi) {
        // 直接使用 Spring AI 标准 OpenAiChatModel
        return new OpenAiChatModel(openAiApi, OpenAiChatOptions.builder()
                .model("qwen-max")
                .temperature(0.7)
                .build());
    }

    @Bean("userChatClient")
    public ChatClient userChatClient(ChatClient.Builder builder, UserAITools userTools) {
        return builder
                .defaultSystem("你是一个智慧校园系统的用户端AI助手...")
                .defaultTools(userTools)
                .build();
    }

    @Bean("adminChatClient")
    public ChatClient adminChatClient(ChatClient.Builder builder, AdminAITools adminTools) {
        return builder
                .defaultSystem("你是一个智慧校园管理系统的AI助手...")
                .defaultTools(adminTools)
                .build();
    }
}
如果必须用自定义实现，则需要在 AlibabaOpenAiChatModel 中从 prompt options 正确提取工具定义：


private OpenAiApi.ChatCompletionRequest addToolDefinitions(
        OpenAiApi.ChatCompletionRequest request, Prompt prompt) {
    
    // 从 prompt 的 runtime options 中获取工具定义
    if (prompt.getOptions() instanceof OpenAiChatOptions chatOptions) {
        if (!CollectionUtils.isEmpty(chatOptions.getToolCallbacks())) {
            // 将 ToolCallback 转换为 FunctionTool
            List<OpenAiApi.FunctionTool> functionTools = chatOptions.getToolCallbacks()
                    .stream()
                    .map(callback -> new OpenAiApi.FunctionTool(
                            new OpenAiApi.FunctionTool.Function(
                                    callback.getDescription(),
                                    callback.getName(),
                                    callback.getInputSchema())))
                    .collect(Collectors.toList());
            return ModelOptionsUtils.merge(
                    OpenAiChatOptions.builder().tools(functionTools).build(),
                    request, OpenAiApi.ChatCompletionRequest.class);
        }
    }
    return request;
}
同时 ToolCallingManager 应该从 Spring 容器中获取，而非手动 new：


// AIConfig 中
@Bean
public ToolCallingManager toolCallingManager() {
    return ToolCallingManager.builder().build();
}
方案 2：修复类型不匹配
SeatReservationServiceImpl 中将接收类型统一为 Integer：


Integer result = redisSeatService.reserveSeat(...);
Integer result = redisSeatService.cancelReservation(...);
方案 3：修复 MyBatis 映射缺失
在 SeatMapper.xml 中添加：


<select id="getAvailableSeatList" resultType="com.hxq.smart_campus.entity.vo.SeatListVO">
    select s.id, s.seat_number as seatNumber, s.status, s.room_id as roomId
    from seat s
    <where>
        <if test="status != null and status != ''">
            s.status = #{status}
        </if>
    </where>
</select>
或者直接在接口上添加 @Select 注解。

方案 4：修复语义问题
AdminAITools.getClassScores 增加按 classId 过滤的逻辑，或配合 ScoreEntryMapper.getScoreListForStatistics 方法使用
UserAITools.searchBooks 改为同时搜索 title 和 author（传入同一个 keyword），或者新建一个支持多字段模糊搜索的 MyBatis 查询
方案 5：清理冗余代码
移除 BookRecommendService 和 CourseRecommendService 中未使用的 aiService 字段
移除 SeatReservationServiceImpl 中未使用的 SeatScheduleVO 和 SEAT_STATUS_FREE 导入
四、优化优先级
优先级	问题	影响	工作量
P0	ToolCallingManager 为空	工具调用完全不可用	1-2h
P0	类型不匹配（Integer/Long）	项目无法编译	5min
P0	getAvailableSeatList 无映射	运行时 MyBatis 异常	5min
P1	工具定义未发送到 API	工具调用完全不可用	2-4h
P2	getClassScores 忽略 classId	查询结果语义错误	30min
P2	searchBooks 不匹配作者	作者搜索无结果	30min
P3	未使用的导入和字段	代码冗余	10min
