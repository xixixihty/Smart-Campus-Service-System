一、总体架构与数据流
text
客户端请求 → Nginx 负载均衡 → Spring Boot 应用 → Redis 集群 + Lua 脚本 + Redisson → MySQL
Redis 集群：主从 + 哨兵模式，数据分片，承担 99% 的读、写请求。

Redisson：实现分布式锁、布隆过滤器、ZSet 延迟队列。

MySQL：仅作数据持久化与最终一致性保证。

二、座位预约模块设计
2.1 数据结构设计
Key	类型	说明
seat:info:{seatId}	Hash	座位基本信息（编号、阅览室、状态等）
seat:available:{date}	Set	当日所有空闲座位 ID
seat:reserved:{date}	Set	当日已预约座位 ID
seat:lock:{seatId}:{date}	String（分布式锁）	锁定座位的互斥锁
seat:schedule:{seatId}:{date}	ZSet	该座位该天的预约时间片（score=起始分钟数）
user:reservation:{userId}	ZSet	用户的预约记录，按预约日期排序
2.2 缓存穿透防护（布隆过滤器）
问题：恶意查询不存在的座位 ID，缓存和数据库都不命中，压力直达 DB。

方案：初始化时将所有有效座位 ID 添加到 Redis 布隆过滤器 bf:seats。每次查询前先检查布隆过滤器，不存在则直接返回“座位不存在”。

java
RBloomFilter<String> filter = redisson.getBloomFilter("bf:seats");
filter.tryInit(100000L, 0.03); // 预估10万座位，误判率3%
filter.add("seat:1001");
2.3 缓存击穿防护（互斥锁 + 逻辑过期）
问题：热门座位（如靠窗位置）在某一时刻缓存过期，大量请求同时打到 DB。

方案 1（互斥锁）：查询缓存未命中时，用 setnx 抢锁，抢到锁的线程加载 DB 并回写缓存。

方案 2（逻辑过期）：在 Hash 中存入一个过期时间字段，发现逻辑过期后，仍返回旧数据，同时异步线程抢锁更新缓存。

java
// 互斥锁示例
String lockKey = "lock:seat:" + seatId;
RLock lock = redisson.getLock(lockKey);
try {
    if (lock.tryLock(3, 10, TimeUnit.SECONDS)) {
        // 查DB并回写缓存
    }
} finally {
    lock.unlock();
}
2.4 缓存雪崩防护
问题：大量座位缓存同时过期，或 Redis 宕机，请求全部打崩 DB。

方案：

过期时间加上随机值，避免集体失效。

构建 Redis 哨兵或集群，保证高可用。

降级策略：Redis 不可用时，返回限流提示或本地缓存数据（Caffeine）。

永不过期 + 逻辑过期策略确保缓存层始终存活。

2.5 座位预约核心流程（分布式锁 + ZSet 时间冲突检测）
选择日期和时间段：用户提交 seatId、date、startTime、endTime。

分布式锁：以 seat:lock:{seatId}:{date} 为 key 加锁，保证同一个座位在同一日期的预约操作串行化。

冲突检测（ZSet）：

计算 startTime 和 endTime 代表的分钟数（如 09:30 = 570）。

在 seat:schedule:{seatId}:{date} 中，使用 ZRANGEBYSCORE 查询是否有时间重叠的预约。

无冲突时，将新预约写入 ZSet：ZADD seat:schedule:1001:2025-05-01 570 600。

更新状态：SADD seat:reserved:2025-05-01 seat:1001；SREM seat:available:2025-05-01 seat:1001。

用户记录：ZADD user:reservation:1001 20250501 "座位A 09:30-10:00"。

释放锁。

全部操作通过 Lua 脚本 原子执行，避免网络往返和并发问题：

lua
-- 检测冲突并预定座位的Lua脚本
local seat_key = KEYS[1]   -- seat:schedule:{seatId}:{date}
local start_score = ARGV[1]
local end_score = ARGV[2]
local member = ARGV[3]

-- 查找是否有交叉时段
local existing = redis.call('ZRANGEBYSCORE', seat_key, start_score, end_score)
if #existing > 0 then
    return 0  -- 冲突
end
-- 无冲突，插入预约
redis.call('ZADD', seat_key, start_score, member)
-- 更新可用/已预约集合（略）
return 1
2.6 ZSet 的更多应用：座位热度排行
统计每个座位被预约的次数：ZINCRBY seat:popular:week 1 seat:1001。

实时获取热门座位 Top10：ZREVRANGE seat:popular:week 0 9 WITHSCORES。

为管理员提供决策依据，并在前台展示“热门座位”。

三、选课模块设计
3.1 数据结构设计
Key	类型	说明
course:info:{courseId}	Hash	课程基本信息、总容量、已选人数
course:stock:{courseId}	String（数字）	剩余名额（预扣库存）
course:selected:{userId}	Set	已选课程集合（防止重复选课）
seckill:order:queue	ZSet	选课请求排队队列（score = 请求时间戳）
course:waiting:{courseId}	ZSet	课程候补队列（按请求时间排序）
3.2 高并发选课流程（分布式锁 + Lua 扣减库存）
预热库存：选课开始前，将课程库存 course:stock:{courseId} 加载到 Redis。

防重检查：SISMEMBER course:selected:{userId} courseId，已选则直接返回。

库存扣减（Lua 原子操作）：

lua
local stock_key = KEYS[1]   -- course:stock:{courseId}
local selected_key = KEYS[2] -- course:selected:{userId}
local stock = tonumber(redis.call('GET', stock_key) or "0")
if stock <= 0 then
    return -1  -- 已满
end
redis.call('DECR', stock_key)
redis.call('SADD', selected_key, ARGV[1])
return 1  -- 成功
异步落库：选课成功后，将消息发送到消息队列（或直接写入 MySQL 事务），确保最终一致性。

候补机制（ZSet）：库存为 0 时，将用户放入 course:waiting:{courseId}，score 为请求时间。当有学生退课时，从 ZSet 中按序通知候补者。

3.3 分布式锁的应用范围
库存初始化：防止多个实例同时从 DB 加载库存。

退课操作：退课 + 候补通知需保证原子，可加锁整个流程或使用 Lua。

课程信息修改：管理员修改容量时需锁定对应课程，避免读写不一致。

3.4 缓存策略细则
问题	对应方案
穿透	布隆过滤器过滤不存在的课程 ID；非法请求直接拦截。
击穿	课程详情缓存永不过期，使用逻辑过期+互斥锁更新。
雪崩	库存等热点数据使用多级缓存（Caffeine + Redis）；Redis 集群高可用；降级限流。
3.5 ZSet 的更多应用：选课排队与公平调度
选课开始瞬间流量涌入，使用 ZSet 实现 排队机制：用户请求先写入 seckill:order:queue，score 为微秒级时间戳，后台异步线程按照 ZSet 顺序处理，避免服务器瞬时过载，同时保证公平性。

四、分布式系统能力清单
知识点	在项目中的落地
缓存穿透	布隆过滤器（bf:seats、bf:courses）
缓存击穿	互斥锁（Redisson.getLock）；逻辑过期
缓存雪崩	过期时间随机化；Redis 集群；本地缓存降级（Caffeine）
布隆过滤器	座位 ID、课程 ID 的存在性校验
ZSet	预约时间冲突检测、预约排序、人气排行、候补队列
分布式锁	座位预约冲突、库存扣减、课程信息修改
Lua 脚本	所有 Redis 复合操作原子化执行
消息队列（MQ）	选课结果异步落库（可引入 RabbitMQ/RocketMQ）
多级缓存	Caffeine（本地）+ Redis（远程），提高稳定性
限流与降级	Sentinel 或 Nginx 限流，提供友好提示
五、总结
该方案将分布式系统的核心知识点全部融入到座位预约和选课两个真实业务场景中，既满足了高并发、高可用要求，又为你的项目实践提供了完整的技术栈演示。所有关键操作均通过 Redis + Lua + Redisson 保证原子性和性能，同时通过布隆过滤器、多级缓存、分布式锁等手段解决了经典的缓存三大问题。这套设计可直接落地到你的 Spring Boot 3.5 项目中。


