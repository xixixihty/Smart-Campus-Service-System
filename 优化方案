智慧校园系统代码审查报告
目录
严重漏洞（Critical）
高危漏洞（High）
中危问题（Medium）
低危问题（Low）
前端Bug
设计问题
总结
1. 严重漏洞（Critical）
1.1 完全缺失权限控制（RBAC缺失）
位置: JwtAuthenticationFilter.java + 所有 31 个Controller

JWT过滤器只验证Token是否有效，从未检查用户角色（userType）。所有 controller/admin/ 下的管理端点和 controller/user/ 下的用户端点没有任何权限区分。

后果: 学生登录后可以：

调用 POST /api/students/admin 创建/修改/删除学生账号
调用 POST /api/teachers/admin 创建/修改/删除教师账号
调用 PUT /api/score-entries/admin 录入/修改任何学生的成绩
调用 PUT /api/students/admin/reSetPassword 重置任何学生/教师的密码
1.2 CORS配置严重不安全
位置: WebMvcConfig.java:12-17


.allowedOriginPatterns("*")
.allowCredentials(true)
allowCredentials(true) 与通配符来源结合，浏览器会允许任意网站发送带认证信息的跨域请求。

1.3 Jackson Redis反序列化RCE风险
位置: RedisConfiguration.java:37-38


objectMapper.activateDefaultTyping(LaissezFaireSubTypeValidator.instance,
        ObjectMapper.DefaultTyping.NON_FINAL);
LaissezFaireSubTypeValidator 不做任何类型校验，配合Redis无密码访问（password: 为空），攻击者若写入恶意数据即可触发远程代码执行。

1.4 AI提示注入攻击面
位置: UserAiController.java:36-37


systemPrompt.append(" 附加上下文：").append(chatDTO.getContext());
context 字段直接来自HTTP请求，未经任何清理拼接进系统提示词，攻击者可注入指令操纵AI行为。

2. 高危漏洞（High）
2.1 IDOR（不安全的直接对象引用） - 多处
位置	问题
CourseSelectionServiceImpl.java:147-148	退课的学生ID校验代码被注释掉
ScoreEntryServiceImpl.java:199-200	查成绩时未校验当前用户是否拥有该成绩记录
LeaveApprovalServiceImpl.java:128-140	取消请假未校验请假条归属
SeatReservationServiceImpl	取消/签到/离开座位未校验预约归属
BorrowRecordServiceImpl.java:89-113	还书操作未校验借阅记录归属
2.2 缺少Token撤销机制
位置: JwtUtils.java

JWT签发后无法撤销，无黑名单机制。Token有效期默认 24小时。账号被锁定后，已有Token仍可继续使用。

2.3 登录接口存在用户枚举 + 无速率限制
位置: AuthServiceImpl.java:51-72

登录返回不同错误信息："学生不存在" vs "密码错误" vs "账号已被锁定"，攻击者可枚举有效账号。同时 /api/auth/login 无任何速率限制。

2.4 Swagger接口文档未受保护
位置: JwtAuthenticationFilter.java:75-76

/swagger-ui/**、/v3/api-docs/** 无需认证即可访问，暴露全部API端点信息。

2.5 AI工具函数缺乏服务端授权
位置: UserAITools.java

AI调用的工具函数（查成绩、查借阅等）接收客户端可控的 studentId 参数，服务端不校验该ID是否等于当前登录用户。提示注入可绕过。

2.6 默认密码硬编码
位置: MessageConstant.java:88


public static final String DEFAULT_PASSWORD = "Password123!";
默认密码直接编译入源码，新建账号若不强制改密则存在严重风险。

2.7 MQ幂等性键在事务之前写入（数据丢失风险）
位置: BorrowRecordConsumer.java, SeatReservationConsumer.java, CourseSelectionConsumer.java

Redis幂等键在 @Transactional 数据库提交之前设置。若事务回滚后消息重投，幂等键导致消息被跳过，操作永久丢失。

2.8 数据库配置不使用SSL
位置: application.yaml


useSSL=false&allowPublicKeyRetrieval=true
数据库连接明文传输，生产环境下可被中间人攻击。

2.9 Redis和RabbitMQ无密码
位置: application.yaml

Redis password: 为空，RabbitMQ使用默认 guest/guest 凭据。

2.10 MyBatis SQL日志输出到stdout
位置: application.yaml:117


log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
生产环境下每条SQL（含参数值）会输出到stdout，造成敏感信息泄露。

2.11 文件上传无校验
位置: AliOSSUtils.java, FileController.java:28-37

无文件类型校验（无content-type/magic bytes检查）
无文件大小限制
未设置Content-Type/Content-Disposition头
攻击者可上传恶意脚本/HTML文件到OSS
2.12 多语句写操作缺少事务
多处Service层进行 INSERT + SELECT 操作时缺少 @Transactional：

BookServiceImpl, StudentServiceImpl, TeacherServiceImpl, CourseServiceImpl, BookCategoryServiceImpl, ClassServiceImpl, ClassroomServiceImpl, CollegeServiceImpl, MajorServiceImpl, MakeupExamServiceImpl, NoticeServiceImpl, CourseScheduleServiceImpl

2.13 SemesterServiceImpl.setCurrentSemester() 缺少事务
两步UPDATE操作（先清除所有学期为非当前，再设置目标为当前）无事务保护，第二步失败则所有学期状态丢失。

2.14 Redis KEYS 命令反模式
6处使用阻塞式 KEYS * 扫描，其中 CourseSelectionServiceImpl 在分布式锁内调用，严重降低吞吐量。

2.15 Lua脚本每次请求从磁盘加载
RedisSeatService, RedisCourseService, RedisBorrowService 中每次请求都调用 loadScript() 从磁盘读取 .lua 文件。

2.16 通知缓存Key不包含分页参数
NoticeServiceImpl.getMyNoticeList() 缓存Key不包含 pageNum/pageSize，翻页返回错误数据。

3. 中危问题（Medium）
3.1 SecurityContext ThreadLocal 在异步/响应式场景不安全
位置: SecurityContext.java

AI Controller使用 WebFlux Flux<ServerSentEvent>，ThreadLocal 不跨线程传播。

3.2 身份证号在前端明文显示
位置: MyInfo.vue（admin和user）


<el-descriptions-item label="身份证号">{{ myInfo.idCard }}</el-descriptions-item>
完整身份证号在个人中心明文展示，建议部分脱敏。

3.3 登录页面硬编码测试账号
位置: Login.vue（admin和user）

页面上直接展示 T2021001 / Password123! 和 S2021001 / Password123!。

3.4 @Valid 注解缺失
31/33个Controller的DTO参数未使用 @Valid 或 @jakarta.validation.Valid 注解。校验Bean Validation未引入。

3.5 选课时间段传的是学期名称而非ID
位置: CourseSelectionPeriod.vue:59-62

发送 semesterName 字符串而非 semesterId 数字，导致外键完整性错误。

3.6 成绩录入表单字段不完整
位置: ScoreEntry.vue

后端有 usualScore/finalScore/totalScore/scorePoint 四个字段，但录入表单只有一个 score 字段。

3.7 前端路由不做Token验证
位置: user-vue/router/index.js

只检查 localStorage 是否有token字符串，不校验token是否过期、是否有效。

3.8 多处空catch块
位置: Login.vue, CourseSelection.vue

catch块为空，用户无法感知操作失败。

3.9 MQ课程选择幂等TTL过短
位置: CourseSelectionConsumer.java

幂等键TTL仅5分钟，延迟消息可能被重复处理。

3.10 AI服务 switchIfEmpty 死代码
AiService.java 中 wrapStreamWithEvents() 的 switchIfEmpty 回退因初始状态事件的存在永远无法触发。

3.11 错误消息泄露内部信息
AlibabaOpenAiChatModel.java:308 将异常消息直接注入返回给用户的JSON中。

3.12 前端API模块调用混乱
user-vue 多处直接调用/admin 后缀接口，且跳过API模块直接使用 request。

3.13 用户仪表盘全为Mock数据
位置: Dashboard.vue

课程数量、借阅数量、GPA等全部硬编码，无真实API调用。

4. 低危问题（Low）
pom.xml 缺少 spring-boot-starter-validation
pom.xml 缺少 Sentinel transport 依赖
hutool-all 依赖过泛，应使用 hutool-crypto
caffeine 依赖无对应配置类，可能未使用
无生产/开发环境配置区分（缺少Spring Profiles）
Sentinel Dashboard端口与应用端口冲突（均为8080）
座位表 Seat.vue 表格列 isEdit/isView 逻辑与CRUD页面不一致
重复表格列定义（CourseSchedule.vue、Course.vue）
字段名拼写错误 ExamupExamId → MakeupExamId
生产环境遗留 console.log
RedisTemplate 多处使用原始类型（raw type）
MQ配置中 @ConditionalOnProperty 不一致
BookRecommendService 热点书取前10的逻辑在排序前过滤，返回错误的"前10"
5. 前端Bug
5.1 座位管理页面完全不可用
位置: admin-vue/views/facility/Seat.vue

添加时 :disabled="!isEdit" 导致输入框禁用（逻辑反了）
添加对话框无提交/取消按钮（footer仅在 isEdit=true 时显示）
查看视图无关闭按钮
5.2 AiChat.vue 完全无法运行
位置: user-vue/views/ai/AiChat.vue

调用 chatStream(msg, 'study', callback1, callback2, callback3) 传的是位置参数，但 ai.js 中函数签名是解构对象 { message, context, ... }，导致所有参数为 undefined，直接报错。

5.3 借阅按钮永不禁用
位置: BookBorrow.vue:68


:disabled="row.availableQuantity <= 0"
后端返回的是 availableCopies 而非 availableQuantity，undefined <= 0 始终为 false。

5.4 图书管理Select使用错误字段名
位置: BorrowRecord.vue:18-20


:label="b.bookName"
后端返回 title 而非 bookName。

6. 设计问题
6.1 AI组件碎片化
4个独立的AI聊天组件（admin-vue/AiAssistant.vue、user-vue/AiAssistant.vue、user-vue/AiChat.vue、user-vue/study/AiAssistant.vue）功能高度重叠但实现不同，AiChat.vue 调用方式完全不兼容。

6.2 学生编辑时客户端反向映射学院/专业
位置: Student.vue:298-339

编辑学生时，先查全部班级（pageSize:1000）→遍历找匹配→查全部专业→遍历找匹配→查全部学院。应后端直接返回。

6.3 README.md 缺失项目文档
6.4 无API版本管理
全部接口无版本前缀（/api/v1/），后续升级困难。

6.5 无统一的错误码体系
各模块返回错误格式不一致，前端难以统一处理。

7. 总结
问题分布
严重程度	数量	关键影响
Critical	4	无权限控制、CORS、反序列化RCE、提示注入
High	20	IDOR、Token不可撤销、用户枚举、文件上传、事务缺失等
Medium	15+	敏感数据泄露、校验缺失、缓存错误、死代码等
Low	10+	代码质量、重复列、拼写错误等
最需要优先解决的问题
实现RBAC权限控制 — 所有安全问题的核心，建议加Spring Security或拦截器
修复CORS配置 — 限定具体允许的域名
修复Redis序列化配置 — 使用白名单类型校验器
添加Token撤销机制 — Redis黑名单或缩短TTL+刷新Token
修复IDOR漏洞 — 所有资源操作加归属校验
添加登录速率限制 — 防暴力破解
添加文件上传校验 — 类型、大小、Content-Type
修复前端Seat.vue和AiChat.vue — 阻塞级功能不可用